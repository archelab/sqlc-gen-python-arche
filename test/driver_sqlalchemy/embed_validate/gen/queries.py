# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
#   sqlc-gen-python-arche v0.5.1
__all__: tuple[str, ...] = (
    "AsyncQuerier",
    "GetBookWithAuthorRow",
)

from test.driver_sqlalchemy.embed_validate.shapes import AuthorSettings
import pydantic
import typing

import sqlalchemy
import sqlalchemy.ext.asyncio

from test.driver_sqlalchemy.embed_validate.gen import models


class GetBookWithAuthorRow(pydantic.BaseModel):
    book_id: int
    author: models.Author
    title: str


GET_BOOK_WITH_AUTHOR: typing.Final[str] = """-- name: GetBookWithAuthor \\:one
SELECT book.book_id,
       author.author_id, author.settings,
       book.title
FROM book
JOIN author ON author.author_id = book.author_id
WHERE book.book_id = :p1\\:\\:integer
"""


_AuthorSettings_adapter: pydantic.TypeAdapter[AuthorSettings] = pydantic.TypeAdapter(AuthorSettings)


class AsyncQuerier:
    def __init__(self, conn: sqlalchemy.ext.asyncio.AsyncConnection):
        self._conn = conn

    async def get_book_with_author(self, *, bid: int) -> GetBookWithAuthorRow | None:
        row = (await self._conn.execute(sqlalchemy.text(GET_BOOK_WITH_AUTHOR), {"p1": bid})).first()
        if row is None:
            return None
        return GetBookWithAuthorRow(
            book_id=row[0],
            author=models.Author(author_id=row[1], settings=_AuthorSettings_adapter.validate_python(row[2])),
            title=row[3],
        )
