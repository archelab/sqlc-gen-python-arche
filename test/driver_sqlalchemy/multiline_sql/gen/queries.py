# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
#   sqlc-gen-python-arche v0.5.1
__all__: tuple[str, ...] = (
    "AsyncQuerier",
)

import typing

import sqlalchemy
import sqlalchemy.ext.asyncio

from test.driver_sqlalchemy.multiline_sql.gen import models


GET_ACCOUNT: typing.Final[str] = """-- name: GetAccount \\:one
SELECT account_id,
       label
FROM account
/* multi-line
   block comment
   inside the SQL */
WHERE account_id = :p1\\:\\:integer
"""


class AsyncQuerier:
    def __init__(self, conn: sqlalchemy.ext.asyncio.AsyncConnection):
        self._conn = conn

    async def get_account(self, *, aid: int) -> models.Account | None:
        row = (await self._conn.execute(sqlalchemy.text(GET_ACCOUNT), {"p1": aid})).first()
        if row is None:
            return None
        return models.Account(
            account_id=row[0],
            label=row[1],
        )
