# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
#   sqlc-gen-python-arche v0.5.2
__all__: tuple[str, ...] = (
    "AsyncQuerier",
)

import typing

import sqlalchemy
import sqlalchemy.ext.asyncio

from test.driver_sqlalchemy.pydantic_models.gen import models


GET_AUDIT_LOG: typing.Final[str] = """-- name: GetAuditLog \\:one
SELECT id, table_name, old_data, created_at
FROM audit_log
WHERE id = :p1
"""

GET_FILE_ATTACHMENT: typing.Final[str] = """-- name: GetFileAttachment \\:one
SELECT file_attachment_id, upload_id, chat_id, metadata, created_at
FROM file_attachment
WHERE file_attachment_id = :p1
"""


class AsyncQuerier:
    def __init__(self, conn: sqlalchemy.ext.asyncio.AsyncConnection):
        self._conn = conn

    async def get_audit_log(self, *, id: int) -> models.AuditLog | None:
        row = (await self._conn.execute(sqlalchemy.text(GET_AUDIT_LOG), {"p1": id})).first()
        if row is None:
            return None
        return models.AuditLog(
            id=row[0],
            table_name=row[1],
            old_data=row[2],
            created_at=row[3],
        )

    async def get_file_attachment(self, *, file_attachment_id: int | None) -> models.FileAttachment | None:
        row = (await self._conn.execute(sqlalchemy.text(GET_FILE_ATTACHMENT), {"p1": file_attachment_id})).first()
        if row is None:
            return None
        return models.FileAttachment(
            file_attachment_id=row[0],
            upload_id=row[1],
            chat_id=row[2],
            metadata=row[3],
            created_at=row[4],
        )
