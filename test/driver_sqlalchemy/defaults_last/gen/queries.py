# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
#   sqlc-gen-python-arche v0.5.3
__all__: tuple[str, ...] = (
    "AsyncQuerier",
)

import typing

import sqlalchemy
import sqlalchemy.ext.asyncio

from test.driver_sqlalchemy.defaults_last.gen import models


GET_ITEM: typing.Final[str] = """-- name: GetItem \\:one
SELECT item_id,
       note,
       quantity,
       description
FROM item
WHERE item_id = :p1\\:\\:integer
"""


class AsyncQuerier:
    def __init__(self, conn: sqlalchemy.ext.asyncio.AsyncConnection):
        self._conn = conn

    async def get_item(self, *, iid: int) -> models.Item | None:
        row = (await self._conn.execute(sqlalchemy.text(GET_ITEM), {"p1": iid})).first()
        if row is None:
            return None
        return models.Item(
            item_id=row[0],
            note=row[1],
            quantity=row[2],
            description=row[3],
        )
