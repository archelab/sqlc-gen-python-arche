# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
#   sqlc-gen-python-arche v0.5.0
__all__: tuple[str, ...] = (
    "AsyncQuerier",
)

import collections.abc
import datetime
import typing

import sqlalchemy
import sqlalchemy.ext.asyncio

from test.driver_sqlalchemy.many_stream.gen import models


LIST_EXPIRED_FILE_ATTACHMENTS: typing.Final[str] = """-- name: ListExpiredFileAttachments \\:many
SELECT file_attachment_id, upload_id, chat_id, extraction_id, user_id, created_at, expires_at
FROM file_attachment
WHERE expires_at IS NOT NULL
  AND expires_at < :p1\\:\\:timestamptz
ORDER BY expires_at ASC
LIMIT :p2\\:\\:integer
"""

LIST_UPLOAD_IDS: typing.Final[str] = """-- name: ListUploadIds \\:many
SELECT upload_id
FROM file_attachment
ORDER BY upload_id ASC
"""


class AsyncQuerier:
    def __init__(self, conn: sqlalchemy.ext.asyncio.AsyncConnection):
        self._conn = conn

    async def list_expired_file_attachments(self, *, now: datetime.datetime, limit_count: int) -> collections.abc.AsyncIterator[models.FileAttachment]:
        result = await self._conn.stream(sqlalchemy.text(LIST_EXPIRED_FILE_ATTACHMENTS), {"p1": now, "p2": limit_count})
        async for row in result:
            yield models.FileAttachment(
                file_attachment_id=row[0],
                upload_id=row[1],
                chat_id=row[2],
                extraction_id=row[3],
                user_id=row[4],
                created_at=row[5],
                expires_at=row[6],
            )

    async def list_upload_ids(self) -> collections.abc.AsyncIterator[str]:
        result = await self._conn.stream(sqlalchemy.text(LIST_UPLOAD_IDS))
        async for row in result:
            yield row[0]
