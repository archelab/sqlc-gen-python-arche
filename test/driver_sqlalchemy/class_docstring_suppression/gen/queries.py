# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.31.1
#   sqlc-gen-python-arche v0.5.3
"""Module containing queries from file queries.sql."""
__all__: tuple[str, ...] = (
    "AsyncQuerier",
)

import typing

import sqlalchemy
import sqlalchemy.ext.asyncio

from test.driver_sqlalchemy.class_docstring_suppression.gen import models


GET_FILE_ATTACHMENT_BY_UPLOAD_ID: typing.Final[str] = """-- name: GetFileAttachmentByUploadId \\:one
SELECT file_attachment_id, upload_id, chat_id, extraction_id, user_id, created_at, expires_at
FROM file_attachment
WHERE upload_id = :p1\\:\\:text
"""


class AsyncQuerier:
    def __init__(self, conn: sqlalchemy.ext.asyncio.AsyncConnection):
        self._conn = conn

    async def get_file_attachment_by_upload_id(self, *, upload_id: str) -> models.FileAttachment | None:
        """Fetch one from the db using the SQL query with `name: GetFileAttachmentByUploadId :one`.

        ```sql
        SELECT file_attachment_id, upload_id, chat_id, extraction_id, user_id, created_at, expires_at
        FROM file_attachment
        WHERE upload_id = $1::text
        ```

        Args:
            upload_id: str.

        Returns:
            Result of type `models.FileAttachment` fetched from the db. Will be `None` if not found.
        """
        row = (await self._conn.execute(sqlalchemy.text(GET_FILE_ATTACHMENT_BY_UPLOAD_ID), {"p1": upload_id})).first()
        if row is None:
            return None
        return models.FileAttachment(
            file_attachment_id=row[0],
            upload_id=row[1],
            chat_id=row[2],
            extraction_id=row[3],
            user_id=row[4],
            created_at=row[5],
            expires_at=row[6],
        )
